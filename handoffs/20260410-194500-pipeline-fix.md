# Session Handoff: Pipeline Fix (Pre-Session 1 of V2.1)

**Date:** 2026-04-10
**Session:** Pipeline fix (between V2 and V2.1 Session 1)
**Plan:** plans/v2.1-refinement-plan.md
**Commit:** e7654c4 (plan placement) + pipeline fixes

## What Was Done

### Root Cause Fixed: Plan Was Invisible to Agents

The V2.1 refinement plan was created in `.cursor/plans/kindasmd_v2.1_refinement_422061f9.plan.md`
-- a hidden folder with a UUID filename. No agent following the README workflow could find it.
This caused a session to execute from raw user instructions instead of the plan (violating
Master Rule 1). The Session 4 handoff's cold-start said only "V2 is complete" with no plan
reference, breaking the pipeline.

### Changes Made

1. **Copied V2.1 plan to `plans/v2.1-refinement-plan.md`** (the Pre-Work step the plan itself required)
2. **Reverted premature Session 1 code changes** (commits 700c2a8 and 312648e) -- all source
   files are back to exactly V2.0 state
3. **Renamed `CLAUDE.md` to `AGENTS.md`** -- agent-agnostic naming
4. **Rewrote `README.md`** -- added explicit Pipeline section with plan rules, flow diagram,
   cold-start format requirements, and a "Do NOT" list covering all known failure modes
5. **Rewrote `handoffs/_template.md`** -- cold-start now has required fields including plan
   filename and exact task; added pipeline checklist
6. **Updated Cursor rule** to enforce plan placement and pipeline discipline

## What Was NOT Done

- V2.1 Session 1 work (typography, default view state, dock launcher) -- NOT started.
  All source files are at V2.0. The plan is ready. Session 1 can start fresh.

## Current State

- Build: passing (V2.0 code, unchanged)
- Code: Clean V2.0 -- all Session 1 changes reverted
- Repo: https://github.com/kindashub/KindasMD, plan at commit e7654c4 + pipeline fix commit

## What To Do Next

Read `plans/v2.1-refinement-plan.md` SESSION 1 and execute exactly as written.

## Cold-Start Message

```
Read ~/MBP-Mods/KindasMD/README.md -- it tells you everything.
Check handoffs/ for the latest session note.
The active plan is at plans/v2.1-refinement-plan.md.
You are starting Session 1: Typography + Foundation (Issues 6, 5, 1).

Your task: Match TextEdit font rendering (Menlo 12pt, lineSpacing 0, brighter dark
text, UserDefaults migration). Fix default view state (all toggles OFF, Edit mode,
@AppStorage->@State, remove viewMode persistence). Diagnose and fix Dock launcher.
The plan has exact file paths, line numbers, and code for every change in SESSION 1.
Key files: Theme.swift, ContentView.swift, ClearlyApp.swift, SettingsView.swift,
ScratchpadContentView.swift, EditorView.swift, ScratchpadEditorView.swift.
```

---

## Pipeline Check

- [x] Plan file exists at `plans/v2.1-refinement-plan.md` and is committed
- [x] Cold-start above names the plan file explicitly
- [x] Cold-start above names the next session and task explicitly
- [x] All changes are committed and pushed
- [x] Build passes (`bash build.sh`)
